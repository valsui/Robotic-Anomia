import brain from './index';

const net = new brain.NeuralNetwork();

interface ITrainingItem {
  input: number[];
  output: number[];
}

net.train<ITrainingItem[]>([
  { input: [1, 0, 0, 0], output: [1] }
]);

const out = net.run<number[]>([1, 0, 0,0]);

const outcast = out as string[];